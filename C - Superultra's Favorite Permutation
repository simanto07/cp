#include <bits/stdc++.h>
using namespace std;

bool isPrime(int x) {
    if (x < 2) return false;
    for (int i = 2; i * i <= x; i++)
        if (x % i == 0) return false;
    return true;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;

    while (t--) {
        int n;
        cin >> n;

        if (n <=4) {
            cout << -1 << "\n";
            continue;
        }
    cout<<1<<" "<<3<<" ";
    for(int i=7;i<=n;i+=2)cout<<i<<" ";
    cout<<5<<" "<<4<<" "<<2<<" ";
    for(int i=6;i<=n;i+=2)cout<<i<<" ";
    cout<<endl;
       
    }
}
