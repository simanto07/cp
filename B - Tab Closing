#include <bits/stdc++.h>
using namespace std;
//
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

//SIMANTO
    int T;
    cin >> T;
    while (T--) {
        long long a, b, n;
        cin >> a >> b >> n;

        long long tl = a / b; 
   
        if (n <= tl || (a==b)) {
            cout << 1 << "\n"; 
        } else {
            cout << 2 << "\n";  
        }
    }

}
