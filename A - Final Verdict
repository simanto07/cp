#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--) {
        int n, x;
        cin >> n >> x;

        long long sum = 0;
        for (int i = 0; i < n; i++) {
            int a;
            cin >> a;
            sum += a;
        }

        // Final value must equal the initial average
        if (sum == 1LL * n * x)
            cout << "YES\n";
        else
            cout << "NO\n";
    }
    return 0;
}
